import random 
desde = 1
hasta = 50
def cociente_lista(lista):
    res=[]
    for _ in lista:
        x=random.choice(lista)
        y=random.choice(lista)
        res.append(cociente([x,y]))
    return res
